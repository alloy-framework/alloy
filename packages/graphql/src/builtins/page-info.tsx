import { namekey } from "@alloy-js/core";

/**
 * Namekey for the canonical `PageInfo` type.
 * Kept in builtins to provide a shared identity without pulling in JSX helpers.
 */
export const PageInfo = namekey("PageInfo");
